<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title><%= title %></title>

    <!-- Fonts -->
    <link href="https://fonts.googleapis.com/css2?family=JetBrains+Mono:wght@400&family=Newsreader:opsz,wght@6..72,400;6..72,600&family=Plus+Jakarta+Sans:wght@300;400;500&display=swap" rel="stylesheet" />

    <!-- Tailwind CSS -->
    <script src="https://cdn.jsdelivr.net/npm/@tailwindcss/browser@4"></script>
  </head>

  <body class="bg-[#FAF8F7] text-[#333] font-sans min-h-screen flex flex-col">
    
    <!-- Header -->
    <header class="bg-white shadow-sm">
      <nav class="flex justify-between items-center px-6 py-4 max-w-7xl mx-auto">
        <!-- Logo -->
        <a href="/" class="text-2xl font-serif text-rose-600 font-bold tracking-tight hover:text-rose-700 transition-all">
          Caelvi
        </a>

        <!-- Nav Links -->
        <ul class="flex items-center gap-6 text-sm font-medium">
          <li>
            <a href="/echoes" class="hover:text-rose-600 transition">Home</a>
          </li>
            <li>
              <a href="/logout" class="w-full px-6 py-2 bg-rose-100 text-rose-800 font-medium rounded-xl hover:bg-rose-200 transition duration-300">Logout</a>
            </li>
        </ul>
      </nav>
    </header>

    <!-- Main Wrapper -->
    <main class="flex-grow w-full max-w-6xl mx-auto px-4 sm:px-4 md:px-6 py-6">
      <% if (flash && flash.length > 0) { %>
        <div class="bg-rose-100 text-rose-800 px-4 py-2 rounded mb-6 text-sm">
          <%= flash[0] %>
        </div>
      <% } %>