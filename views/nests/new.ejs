<%- include('../headers/echo', { title: 'Create Nest' }) %>


<div class="min-h-screen bg-[#FDFBFA] flex items-center justify-center px-4 py-12">
  <div class="bg-white shadow-xl rounded-2xl p-8 w-full max-w-md">
    <!-- Title -->
    <h1 class="text-2xl font-serif text-center text-rose-800 font-bold mb-2 tracking-tight">
      🌸 Create a New Nest
    </h1>
    <p class="text-sm text-gray-500 text-center mb-6 italic">
      Your cozy space to gather and cherish echo.
    </p>
    <!-- Form -->
    <form action="/nest/new" method="POST" class="space-y-5">
      <!-- Title Field -->
      <div>
        <label for="title" class="block text-sm font-medium text-gray-700">Nest Title</label>
        <input 
          type="text" 
          name="title" 
          id="title" 
          required
          class="mt-1 block w-full rounded-xl border border-gray-300 p-2 shadow-sm focus:outline-none focus:ring-2 focus:ring-rose-300 focus:border-rose-400"
        />
      </div>
      <!-- Description Field -->
      <div>
        <label for="description" class="block text-sm font-medium text-gray-700">
          Description <span class="text-xs text-gray-400">(Optional)</span>
        </label>
        <textarea 
          name="description" 
          id="description" 
          rows="3"
          class="mt-1 block w-full rounded-xl border border-gray-300 p-2 shadow-sm focus:outline-none focus:ring-2 focus:ring-rose-300 focus:border-rose-400 resize-none"
          placeholder="A cozy space for ideas, art, or dreams..."
        ></textarea>
      </div>
      <!-- Submit -->
      <div class="text-right">
        <button 
          type="submit"
          class="bg-rose-400 hover:bg-rose-500 text-white font-medium py-2 px-5 rounded-xl transition shadow-sm"
        >
          🩹 Create Nest
        </button>
      </div>
    </form>
  </div>
</div>


<%- include('../footers/footer') %>
